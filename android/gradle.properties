org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=384m -XX:+UseG1GC -XX:ReservedCodeCacheSize=128m
android.useAndroidX=true
org.gradle.parallel=false
org.gradle.daemon=false
android.enableR8.fullMode=false
org.gradle.configureondemand=false
# Opt out of AGP 9+ new DSL interface to maintain compatibility with
# audioplayers_android which still uses the classic Groovy build.gradle format.
android.newDsl=false
